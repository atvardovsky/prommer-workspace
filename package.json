{
  "name": "prommer-linkedin-workflow",
  "version": "0.1.0",
  "private": true,
  "description": "A provider-neutral, multi-agent LinkedIn content workflow for prommer.net.",
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "check": "npm run format:check && npm run typecheck && npm test",
    "demo": "tsx src/cli.ts --fixture tests/fixtures/article.html --out artifacts/demo",
    "format": "prettier --write src tests README.md package.json tsconfig.json tsconfig.build.json",
    "format:check": "prettier --check src tests README.md package.json tsconfig.json tsconfig.build.json",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "cheerio": "1.2.0",
    "zod": "4.5.4"
  },
  "devDependencies": {
    "@types/node": "26.5.0",
    "prettier": "3.9.6",
    "tsx": "4.23.13",
    "typescript": "5.9.3",
    "vitest": "5.0.0"
  }
}
